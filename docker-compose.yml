version: '3.7'

services:
  frontend:
    image: jmanche/mern-frontend:${IMAGE_TAG}
    build:
      context: ./frontend
      args:
        NODE_ENV: production
    environment:
      CUSTOMER_DOMAIN: mern-admin.justinmanche.com
      ADMIN_DOMAIN: mern-customer.justinmanche.com
      API_HOST: backend
      API_PORT: 4000
    ports:
      - 80:80
    depends_on:
      - backend

  backend:
    image: jmanche/mern-backend:${IMAGE_TAG}
    build: .
    environment:
      MONGODB_HOST: mongo
      MONGODB_PORT: 27017
      MONGODB_DB: mern
      PORT: 4000
      SESSION_SECRET: mern
      NODE_ENV: production
      ADMIN_DOMAIN: http://mern-admin.justinmanche.com
      CUSTOMER_DOMAIN: http://mern-customer.justinmanche.com
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
